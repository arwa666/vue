const app = Vue.createApp({
    //finction inside the object that returns an (object)
    data(){
        return {
            //The data we make here are accessible from our templates
        }
    }
    
})